# LIST_DETAIL - Visual Architecture

## 📐 **Layout Structure Comparison**

### **Regular LIST Layout**
```
┌─────────────────────────────────────┐
│ TopBar                              │
├─────────────────────────────────────┤
│                                     │
│  content: [                         │
│    { textField },                   │
│    { repeater }                     │
│  ]                                  │
│                                     │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

### **LIST_DETAIL Layout (Phone)**
```
┌─────────────────────────────────────┐
│ TopBar                              │
├─────────────────────────────────────┤
│                                     │
│  masterPane: [                      │
│    { textField },                   │
│    { repeater }                     │
│  ]                                  │
│                                     │
│  (detailPane shown on separate      │
│   screen after navigation)          │
│                                     │
└─────────────────────────────────────┘
```

### **LIST_DETAIL Layout (Tablet)**
```
┌──────────────────────────────────────────────────────────┐
│ TopBar                                                   │
├───────────────────┬──────────────────────────────────────┤
│                   │                                      │
│  masterPane:      │  detailPane:                         │
│  [360dp]          │  [remaining space]                   │
│                   │                                      │
│  { textField }    │  [When selectedDetailData == null]  │
│                   │                                      │
│  { repeater }     │  { emptyState }                      │
│    - Item 1       │  "Select an item..."                 │
│    - Item 2 ←─────┼─→ Selected                           │
│    - Item 3       │                                      │
│                   │  [When selectedDetailData != null]   │
│                   │                                      │
│                   │  { card: "Header" }                  │
│                   │  { card: "Vendor Info" }             │
│                   │  { card: "Lines" }                   │
│                   │  { card: "Actions" }                 │
│                   │                                      │
└───────────────────┴──────────────────────────────────────┘
```

---

## 🔄 **Data Flow Diagram**

### **Phone (Compact Screen)**

```
┌─────────────────────────────────────────────────────────┐
│  Backend API                                            │
└─────────────────────────────────────────────────────────┘
         │                           │
         │ (1)                       │ (3)
         │ GET /pages/               │ GET /pages/
         │ warehouse_receipt_        │ receipt_detail_{id}
         │ list_detail               │
         ▼                           ▼
┌──────────────────┐        ┌──────────────────┐
│  Master Page     │        │  Detail Fragment │
│  JSON            │        │  JSON            │
└──────────────────┘        └──────────────────┘
         │                           │
         │ (2)                       │ (4)
         │ Render                    │ Render
         │ masterPane                │ content
         ▼                           ▼
┌──────────────────┐        ┌──────────────────┐
│  📱 Screen 1     │        │  📱 Screen 2     │
│  ┌────────────┐  │        │  ┌────────────┐  │
│  │ Search     │  │        │  │ ← Back     │  │
│  │ [        ] │  │        │  │            │  │
│  │ ┌────────┐ │  │        │  │ WR-001     │  │
│  │ │ WR-001 │ │──┼────────┼─→│ Details    │  │
│  │ └────────┘ │  │ Click  │  │            │  │
│  │ ┌────────┐ │  │        │  │ [Vendor]   │  │
│  │ │ WR-002 │ │  │        │  │ [Lines]    │  │
│  │ └────────┘ │  │        │  │ [Actions]  │  │
│  └────────────┘  │        │  └────────────┘  │
└──────────────────┘        └──────────────────┘
```

### **Tablet (Expanded Screen)**

```
┌─────────────────────────────────────────────────────────┐
│  Backend API                                            │
└─────────────────────────────────────────────────────────┘
         │                           │
         │ (1)                       │ (3) Optional
         │ GET /pages/               │ GET /fragments/
         │ warehouse_receipt_        │ receipt_detail_{id}
         │ list_detail               │ (for dynamic loading)
         ▼                           ▼
┌──────────────────┐        ┌──────────────────┐
│  Master Page     │        │  Detail Data     │
│  JSON            │        │  JSON            │
│  - masterPane    │        │  (or from        │
│  - detailPane    │        │   selected item) │
└──────────────────┘        └──────────────────┘
         │                           │
         │ (2)                       │ (4)
         │ Render both panes         │ Update detailPane
         │ in single screen          │ with selected data
         ▼                           ▼
┌─────────────────────────────────────────────┐
│  💻 Single Screen (Two Panes)               │
│  ┌──────────┬──────────────────────────┐   │
│  │ Master   │ Detail                   │   │
│  │ [360dp]  │ [flex]                   │   │
│  │ Search   │ WR-001                   │   │
│  │ [      ] │ Status: Open             │   │
│  │ ┌──────┐ │ ──────────────────────   │   │
│  │ │WR-001│─┼→ Vendor Information      │   │
│  │ └──────┘ │ Receipt Lines            │   │
│  │ ┌──────┐ │ Actions                  │   │
│  │ │WR-002│ │ [Post] [Edit] [Delete]   │   │
│  │ └──────┘ │                          │   │
│  └──────────┴──────────────────────────┘   │
└─────────────────────────────────────────────┘
         │
         │ (5) Click on WR-002
         ▼
┌─────────────────────────────────────────────┐
│  💻 Same Screen (State Update)              │
│  ┌──────────┬──────────────────────────┐   │
│  │ Master   │ Detail                   │   │
│  │ [360dp]  │ [flex]                   │   │
│  │ Search   │ WR-002                   │   │
│  │ [      ] │ Status: Closed           │   │
│  │ ┌──────┐ │ ──────────────────────   │   │
│  │ │WR-001│ │ Vendor Information       │   │
│  │ └──────┘ │ Receipt Lines            │   │
│  │ ┌──────┐ │ Actions                  │   │
│  │ │WR-002│─┼→ [Edit] [Delete]         │   │
│  │ └──────┘ │ (No Post - it's closed)  │   │
│  └──────────┴──────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## 🎯 **Action Flow: SELECT_ITEM**

### **Sequence Diagram**

```
Phone:
User         Renderer        NavController    DetailScreen
 │              │                  │               │
 │ Click Item   │                  │               │
 ├─────────────→│                  │               │
 │              │ Detect SELECT_   │               │
 │              │ ITEM action      │               │
 │              ├─────────────────→│               │
 │              │ navigate("de-    │               │
 │              │ tail/WR-001")    │               │
 │              │                  │ Create new    │
 │              │                  │ screen        │
 │              │                  ├──────────────→│
 │              │                  │               │
 │              │                  │  Fetch detail │
 │              │                  │  JSON         │
 │              │                  │               │
 │              │                  │  Render       │
 │              │                  │  content      │
 │              │                  │               │

Tablet:
User         Renderer        State              DetailPane
 │              │               │                   │
 │ Click Item   │               │                   │
 ├─────────────→│               │                   │
 │              │ Detect SELECT_│                   │
 │              │ ITEM action   │                   │
 │              │ Update state  │                   │
 │              ├──────────────→│                   │
 │              │ selectedDetail│                   │
 │              │ Data = {data} │                   │
 │              │               │ Trigger recomp.   │
 │              │               │ with new data     │
 │              │               ├──────────────────→│
 │              │               │                   │
 │              │               │   Re-render with  │
 │              │               │   new data        │
 │              │               │                   │
```

---

## 📦 **JSON Structure Tree**

```
PageDefinition (warehouse-receipt-list-detail-page.json)
│
├── meta
│   ├── title: "Warehouse Receipts"
│   ├── pageType: "LIST"
│   └── cachePolicy: "NETWORK_FIRST"
│
├── layout
│   ├── type: "LIST_DETAIL"  ← KEY: Enables adaptive behavior
│   │
│   └── areas
│       ├── topBar
│       │   ├── title: "Warehouse Receipts"
│       │   ├── showBackButton: false
│       │   └── actions: [refresh]
│       │
│       ├── masterPane  ← Rendered on left (or full screen on phone)
│       │   ├── [0] TextFieldComponent (search)
│       │   └── [1] RepeaterComponent
│       │       ├── layoutType: "COLUMN"
│       │       ├── itemTemplate: CardComponent
│       │       │   ├── fields: [receiptNo, vendor, status]
│       │       │   └── action: SELECT_ITEM  ← Triggers selection
│       │       │
│       │       ├── emptyState: EmptyStateComponent
│       │       └── dataSource
│       │           ├── endpoint: "/api/v1/warehouse/receipts"
│       │           └── keyField: "receiptNo"
│       │
│       ├── detailPane  ← Rendered on right (or separate screen)
│       │   └── [0] EmptyStateComponent
│       │       ├── icon: "info"
│       │       └── message: "Select a receipt..."
│       │
│       └── floatingAction
│           ├── icon: "filter_list"
│           └── action: SHOW_FILTERS
│
└── dataBinding
    ├── endpoint: "/api/v1/warehouse/receipts"
    ├── method: "GET"
    └── keyField: "receiptNo"


DetailFragment (warehouse-receipt-detail-fragment.json)
│
├── meta
│   ├── title: "Receipt Details"
│   ├── parentPage: "warehouse_receipt_list_detail"
│   └── targetPane: "detailPane"
│
├── content  ← Array of components (no layout areas)
│   ├── [0] CardComponent (header)
│   │   └── fields: [receiptNo, status]
│   │
│   ├── [1] CardComponent (vendor section)
│   │   ├── title: "Vendor Information"
│   │   └── fields: [vendorNo, vendorName, postingDate]
│   │
│   ├── [2] CardComponent (lines section)
│   │   ├── title: "Receipt Lines"
│   │   └── fields: [RepeaterComponent]
│   │       └── itemTemplate: CardComponent
│   │           └── fields: [itemNo, description, qty, cost]
│   │
│   └── [3] CardComponent (actions)
│       └── fields: [Post, Edit, Delete buttons]
│
└── dataBinding
    ├── endpoint: "/api/v1/warehouse/receipts/{receiptNo}"
    └── keyField: "receiptNo"
```

---

## 🔧 **Renderer Logic Flow**

```
ConsumePage(pageDefinition)
    │
    ├─ Is layout.type == LIST_DETAIL?
    │  │
    │  ├─ YES
    │  │  │
    │  │  ├─ Is screen EXPANDED (tablet)?
    │  │  │  │
    │  │  │  ├─ YES → ConsumeListDetailLayout()
    │  │  │  │           │
    │  │  │  │           ├─ Render Row
    │  │  │  │           │   ├─ Column (360dp)
    │  │  │  │           │   │   └─ Render masterPane components
    │  │  │  │           │   │
    │  │  │  │           │   ├─ Divider (1dp)
    │  │  │  │           │   │
    │  │  │  │           │   └─ Box (weight 1f)
    │  │  │  │           │       ├─ If selectedDetailData != null
    │  │  │  │           │       │   └─ Render detailPane with data
    │  │  │  │           │       │
    │  │  │  │           │       └─ Else
    │  │  │  │           │           └─ Render empty state
    │  │  │  │           │
    │  │  │  │           └─ onAction handler
    │  │  │  │               └─ If SELECT_ITEM
    │  │  │  │                   └─ selectedDetailData = data
    │  │  │  │
    │  │  │  └─ NO → ConsumeMasterPane()
    │  │  │            │
    │  │  │            ├─ Render Column
    │  │  │            │   └─ Render masterPane components
    │  │  │            │
    │  │  │            └─ onAction handler
    │  │  │                └─ If SELECT_ITEM
    │  │  │                    └─ navController.navigate(detail/id)
    │  │  │
    │  └─ NO → Render standard Column
    │           └─ Render content components
    │
    └─ Scaffold with topBar, FAB, etc.
```

---

## 📱 **Responsive Breakpoints**

```
Screen Width             Layout Behavior
─────────────────────────────────────────────
0dp                     │
  ↓                     │ Phone
600dp                   │ (Single Pane)
─────────────────────   ├───────────────
COMPACT                 │
─────────────────────   │
840dp                   │
  ↓                     │ Tablet
1240dp                  │ (Two Panes)
─────────────────────   ├───────────────
EXPANDED                │
─────────────────────   │
```

**Detection:**
```kotlin
val windowSizeClass = currentWindowAdaptiveInfo().windowSizeClass
val isTablet = windowSizeClass.windowWidthSizeClass == WindowWidthSizeClass.EXPANDED
```

---

**Perfect! Your SDUI system now fully supports adaptive LIST_DETAIL layouts! 🎉**
